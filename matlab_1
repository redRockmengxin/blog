多行输入
1.fun ;
2.fun shirt+enter fun

数据类型
数值类型、逻辑类型、字符串、函数句柄、结构体、单元数组

单精度不能和整数进行算术运算

//WHO列出当前变量，长格式
whos是WHO的一个长形式。它列出了当前数据库中的所有变量
工作区，以及有关其大小、字节、类的信息等

eps是MATLAB中区别两个浮点数的一个量度。
eps可获取一个数值和最接近该数值浮点数的间隙
n越接近0，结果越小

复数（complex）
无穷量（Inf和-Inf）
NaN非数值量
非数属于双精度浮点数

逻辑类型
a=1:9是变量从整数1取到9
10：-1：7是反过来
与（&）、或（|）、非（~）

异与(xor):xor(x,y)
xy同真和同假是假否则是真
向量=元素加地址（下标索引）
现截止到26：函数句柄
